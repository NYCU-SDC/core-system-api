FROM node:24-alpine

WORKDIR /app

RUN corepack enable

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .

RUN pnpm compile:openapi

EXPOSE 4010

CMD ["pnpm", "exec", "prism", "mock", "tsp-output/schema/openapi.1.0.0.yaml", "--host", "0.0.0.0", "--port", "4010"]
