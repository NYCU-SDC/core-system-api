@userId = {{LOGIN_USER_ID}}

###

# @title Login with OAuth Provider
# @description Login with OAuth2 provider. This initiates the OAuth flow and redirects to the provider's login page. See details: https://clustron.atlassian.net/wiki/spaces/CS/pages/41320449/Authentication+Process
POST {{BASE_URL}}/auth/login/internal
{
    "uid": "{{userId}}"
}

###

# @title Refresh Access Token
# @description Refresh the access token using a refresh token. Returns a new access token, expiration time, and refresh token.
POST {{BASE_URL}}/auth/refresh/{{refreshToken}}

###

# @title Logout User
# @description Logout the current authenticated user. Requires authentication.
POST {{BASE_URL}}/auth/logout
